import * as math from "mathjs";


export default function getNthRoot() {
  let test = "nthRoot(e)"
  // console.log(test.indexOf("pi"));
  // switch(test)

  console.log(`test sqrt pi: ${math.evaluate(test)}`);

}